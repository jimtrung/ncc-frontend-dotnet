            </StackPanel>

            <DataGrid x:Name="auditoriumTable" Style="{StaticResource data-grid-dark}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      GridLinesVisibility="None"
                      HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn x:Name="nameColumn" Header="Tên phòng" />
                    <DataGridTextColumn x:Name="typeColumn" Header="Loại phòng" />
                    <DataGridTextColumn x:Name="capacityColumn" Header="Số lượng" />
                    <DataGridTextColumn x:Name="noteColumn" Header="Ghi chú" />
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Content="Delete all" x:Name="deleteAllBtn" Style="{StaticResource button-danger}"/>
                <Button Content="Add Auditorium" x:Name="addAuditoriumBtn" Style="{StaticResource button-primary}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
